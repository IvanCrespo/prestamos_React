import React, { Fragment } from 'react'

/* function Header({titulo}) {

    return (
        <Fragment>
            <h1>{titulo}</h1>
        </Fragment>
    )
} */

const Header = ({ titulo }) => {
    // const total = 1 + 2;
    return (
        <Fragment>
            <h1>{titulo}</h1>
        </Fragment>
    );
}

export default Header;